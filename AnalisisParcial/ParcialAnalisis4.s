;Delay Slot: Ejecuta siempre la siguiente instruccion despues del salto

;Describir que funciones cumple la etapa IF:
;Se toma la instruccion(codigo binario), incrementa el PC(ip) y se anazaliza
;el Brench Target Buffer.